# Production Environment Variables for Railway
# These should be set in Railway dashboard, not in this file

# Database (Railway will provide this automatically)
# DATABASE_URL=mysql://username:password@host:port/database

# JWT Secret (Set a strong secret in Railway)
# JWT_SECRET=your_super_secure_jwt_secret_key_here_32_chars_min

# Frontend URL (Vercel deployment URL)
# FRONTEND_URL=https://the-jewellery-store.vercel.app

# Node Environment
# NODE_ENV=production
